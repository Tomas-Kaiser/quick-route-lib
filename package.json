{
  "name": "quick-route",
  "version": "1.0.0",
  "description": "Quick route is used to parsing partial addresses.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "build:types": "tsc --project tsconfig.json",
    "build": "vite build && npm run build:types",
    "test": "vitest",
    "test:cov": "vitest --coverage",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "quick-route"
  ],
  "author": "Tomas",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@types/node": "^22.13.1",
    "@vitest/coverage-v8": "^3.0.5",
    "install": "^0.13.0",
    "jsdoc": "^4.0.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "joi": "^17.13.3"
  }
}
